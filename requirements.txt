# Lost Meadows Detection - Python Requirements
#
# NOTE: For geospatial projects, we STRONGLY recommend using conda (environment.yml)
# instead of pip, as it handles GDAL and rasterio dependencies much better.
#
# If you must use pip:
#   pip install -r requirements.txt
#
# But conda is easier:
#   conda env create -f environment.yml

# Core geospatial (requires GDAL system library)
# IMPORTANT: Install GDAL system library first!
# Ubuntu: sudo apt install gdal-bin libgdal-dev
# macOS: brew install gdal
GDAL>=3.6.0,<4.0.0
rasterio>=1.3.0,<2.0.0
Fiona>=1.9.0,<2.0.0
geopandas>=0.14.0,<1.0.0

# Scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.11.0,<2.0.0
pandas>=2.0.0,<3.0.0

# Machine learning
scikit-learn>=1.3.0,<2.0.0
joblib>=1.3.0,<2.0.0

# Parallel processing (requires MPI system library)
# Ubuntu: sudo apt install mpich
# macOS: brew install open-mpi
mpi4py>=3.1.0,<4.0.0

# Utilities
tqdm>=4.66.0

# Optional: Development tools
# jupyter>=1.0.0
# ipython>=8.14.0
# matplotlib>=3.7.0

# Optional: Google Earth Engine
# earthengine-api>=0.1.0
